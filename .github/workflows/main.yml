name: Trivi Scan
on:
  workflow_call: 

jobs:
    scan:
        runs-on: ubuntu-latest
        steps:
          - name: Print Hello World
            run: |
              echo "Hello World!"          



    rodolfo:
      name: rodolfo 
      uses: ./.github/workflows/actions/java/action.yml